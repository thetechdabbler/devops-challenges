# ============================================================
# DevOps Practice Portal — Environment Configuration
# Copy this file to .env and fill in real values.
# NEVER commit .env to version control.
# ============================================================

# PostgreSQL credentials (used by docker-compose.yml and backend)
POSTGRES_USER=portaluser
POSTGRES_PASSWORD=change_me_use_strong_random_password

# JWT signing secret — generate with: openssl rand -hex 32
JWT_SECRET=change_me_64_char_hex_string

# GitHub OAuth App credentials
# Create at: https://github.com/settings/developers → OAuth Apps
# Use TWO separate OAuth Apps — one for dev, one for prod
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
# Must match the Authorization callback URL in your GitHub OAuth App settings exactly
# Dev:  http://localhost:3001/auth/github/callback
# Prod: https://your-domain.com/auth/github/callback
GITHUB_CALLBACK_URL=https://your-domain.com/auth/github/callback

# Application config
FRONTEND_URL=https://your-domain.com
PORT=3001
NODE_ENV=production
LOG_LEVEL=info

# For local development, use:
# FRONTEND_URL=http://localhost:5173
# NODE_ENV=development
# LOG_LEVEL=debug
