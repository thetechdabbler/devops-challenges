# Local development — plain HTTP, no TLS
http://localhost {
    handle /api/* {
        reverse_proxy backend:3001
    }
    handle /auth/* {
        reverse_proxy backend:3001
    }
    handle /health {
        reverse_proxy backend:3001
    }
    handle {
        reverse_proxy frontend:80
    }
}
