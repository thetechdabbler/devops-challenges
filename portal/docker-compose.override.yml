# Local development overrides — auto-merged by `docker compose up`.
# NOT used in production (production runs docker compose -f docker-compose.yml up).
services:
  caddy:
    volumes:
      - ./Caddyfile.dev:/etc/caddy/Caddyfile:ro
    ports:
      - "80:80"
      # 443 not needed locally; remove binding to avoid ERR_SSL_PROTOCOL_ERROR
