server {
    listen 80;                     # BUG 3: hardcoded port — should use {{ http_port }}
    server_name {{ server_name }};

    location / {
        proxy_pass http://127.0.0.1:5000;
    }
}
