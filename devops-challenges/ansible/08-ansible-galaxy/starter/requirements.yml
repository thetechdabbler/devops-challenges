# BUG 1: wrong format — missing 'roles:' wrapper list
name: geerlingguy.nginx        # these are top-level keys, not a list
version: "3.2.0"
---
name: geerlingguy.mysql
src: github.com/geerlingguy/ansible-role-mysql   # BUG 2: missing https:// prefix
version: "3.4.0"
