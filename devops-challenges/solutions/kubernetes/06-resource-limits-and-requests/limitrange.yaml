apiVersion: v1
kind: LimitRange
metadata:
  name: dev-limits
spec:
  limits:
    - type: Container
      default:
        memory: 128Mi
        cpu: 500m
      defaultRequest:
        memory: 64Mi
        cpu: 100m
      max:
        memory: 512Mi
        cpu: "2"
      min:
        memory: 16Mi
        cpu: 10m
