global:
  scrape_interval: 15s      # FIX 1: add unit suffix
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "node"             # FIX 4: unique job name
    metrics_path: /metrics       # FIX 2: plural /metrics
    honor_labels: true           # FIX 5: at job level, not under static_configs
    static_configs:
      - targets: ["localhost:9100"]  # FIX 3: list format
