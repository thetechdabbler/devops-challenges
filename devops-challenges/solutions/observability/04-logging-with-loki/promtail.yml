server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml    # FIX 5: absolute path

clients:
  - url: http://loki:3100/loki/api/v1/push   # FIX 1: correct path with loki/ prefix

scrape_configs:
  - job_name: app
    static_configs:
      - targets: [localhost]
        labels:                    # FIX 3: add labels block
          job: devops-app
          __path__: /var/log/*.log

    pipeline_stages:
      - json:                      # FIX 2: correct stage name
          expressions:
            level: level
            msg: message
      - labels:
          level:
