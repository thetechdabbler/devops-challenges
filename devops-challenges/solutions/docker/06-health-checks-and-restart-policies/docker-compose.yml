version: "3.9"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped

  worker:
    image: busybox
    command: sh -c "echo 'worker started' && sleep infinity"
    depends_on:
      app:
        condition: service_healthy
    restart: unless-stopped
