apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
data:
  # BUG 3: values in data: must be base64 encoded.
  # Use stringData: instead (Kubernetes auto-encodes), or run:
  #   echo -n "super-secret-password" | base64
  db-password: super-secret-password   # this will cause an error — not valid base64
