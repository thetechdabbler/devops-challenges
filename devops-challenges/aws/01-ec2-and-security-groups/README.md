# Exercise 01 — EC2 and Security Groups

Fix an AWS CLI script that launches an EC2 instance with a security group.

## Quick Start

```bash
# Configure AWS CLI first
aws configure

# Validate your fixes (dry-run)
bash starter/launch.sh --dry-run
```

## Files

```
starter/
  launch.sh        — EC2 launch script (5 bugs)
  userdata.sh      — Bootstrap script
solutions/
  launch.sh        — fixed script
  step-by-step-solution.md
```
