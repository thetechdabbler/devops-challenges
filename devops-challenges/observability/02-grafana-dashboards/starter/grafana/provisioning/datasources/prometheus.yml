apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus-datasource   # BUG 1: should be "prometheus"
    access: direct                # BUG 2: should be "proxy"
    url: http://prometheus:9090
    uid: prometheus-ds
    isDefault: true
