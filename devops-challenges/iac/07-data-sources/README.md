# Exercise 07 — Data Sources

Fix a Terraform configuration that uses data sources to look up an existing AMI and VPC instead of hardcoding IDs.

## Quick Start

```bash
terraform init
terraform validate
terraform plan
```

## Files

```
starter/
  main.tf          — data source config (5 bugs)
solutions/
  main.tf          — fixed config
  step-by-step-solution.md
```
