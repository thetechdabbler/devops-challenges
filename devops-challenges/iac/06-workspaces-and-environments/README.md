# Exercise 06 — Workspaces and Environments

Fix a workspace-aware Terraform configuration that selects instance types and counts based on the active workspace.

## Quick Start

```bash
terraform init
terraform workspace new staging
terraform validate
terraform plan
```

## Files

```
starter/
  main.tf          — workspace-aware config (5 bugs)
solutions/
  main.tf          — fixed config
  step-by-step-solution.md
```
