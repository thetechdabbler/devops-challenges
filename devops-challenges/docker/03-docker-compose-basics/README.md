# 03 — Docker Compose Basics

**Level**: Beginner | **Topic**: Docker

Run the Flask app and Redis together with a single command by completing the provided `docker-compose.yml`.

## Quick Start

```bash
cp ../../../../shared-resources/app/app.py starter/
cp ../../../../shared-resources/app/requirements.txt starter/
cd starter
# Complete the TODOs in docker-compose.yml, then:
docker compose up
curl http://localhost:5000/health
```

See [`challenge.md`](./challenge.md) for full instructions.

## Solution

[`solutions/docker/03-docker-compose-basics/`](../../../solutions/docker/03-docker-compose-basics/)
